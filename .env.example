# URL dasar API backend Laravel (akhiri dengan /api)
VITE_API_URL=http://localhost:8000/api

# Folder media backend, dipakai Vite saat `npm run dev` untuk menyajikan
# /storage langsung dari disk (mendukung header Range, jadi video tidak
# tersendat). Isi hanya bila repo backend tidak bersebelahan dengan repo ini.
# VITE_STORAGE_DIR=../BE-Display-RSIJPK/storage/app/public
